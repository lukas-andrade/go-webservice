# Datasources come from ../grafana-datasources.yaml, shared with docker
# compose. Anonymous admin is only acceptable because this runs on a laptop.
grafana.ini:
  auth:
    disable_login_form: true
  auth.anonymous:
    enabled: true
    org_role: Admin

sidecar:
  dashboards:
    enabled: true
    label: grafana_dashboard
